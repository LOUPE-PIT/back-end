version: '3.4'

services:

  db:
    image: "mcr.microsoft.com/mssql/server:2019-latest"
    container_name: db
    hostname: db
    environment:
        SA_PASSWORD: "Welkom12345"
        ACCEPT_EULA: "Y"

  synchronizationservice.api:
    depends_on: ["db"]
    image: ${DOCKER_REGISTRY-}synchronizationserviceapi
    ports: 
      - "7241:7241"
    build:
      context: .
      dockerfile: SynchronizationService.API/Dockerfile